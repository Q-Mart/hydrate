{
  "name": "hydrate",
  "version": "1.0.0",
  "description": "Hydra for use with Text Editors",
  "main": "app.js",
  "scripts": {
    "build": "browserify ./tmp.js --debug -o ./bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hydra-synth": "^1.3.6"
  },
  "devDependencies": {
    "browserify": "^17.0.0"
  }
}
